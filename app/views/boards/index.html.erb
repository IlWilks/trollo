<h1>Boards</h1>
<div class= "ui grid container">
<% @boards.each do |board| %>
  <div class="eight wide column">
    <div class="ui segment" style="background-color: #4bc437;color: #FFFFFF">
      <h3><%= link_to board.name, board, class: "link_color" %></h3>
    </div>
  </div>
<% end %>
</div>